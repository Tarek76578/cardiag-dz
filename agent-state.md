# Autonomous Agent State

The autonomous engineering workflow is installed in `.github/workflows/cardiag-agent.yml`.

Current milestone: stabilize CI and Android runtime, then continue through security, data integrity, UX, and release readiness.

The agent must use evidence from CI/runtime results and must never weaken tests to obtain green status.
