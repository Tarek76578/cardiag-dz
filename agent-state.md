# Autonomous Agent State

The autonomous engineering workflow is installed in `.github/workflows/cardiag-agent-safe.yml` and the Android project root is `android/`.

Current mission: complete CarDiag as a professional production Android automotive diagnostics product for the Algerian market, not merely make CI green.

The agent must continue across scheduled cycles using evidence from CI/runtime results and the Definition of Done in `AGENTS.md`. It must never weaken, skip, or delete tests/checks to obtain green status.

Current immediate blocker: verify the corrected agent workflow reaches Codex successfully, then resolve the latest CI/test failures and continue through runtime, security, data integrity, UX/UI, OBD readiness, localization, and release readiness.
