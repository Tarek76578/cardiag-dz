name: CarDiag FreeLLMAPI Agent

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: cardiag-freellmapi-agent
  cancel-in-progress: false

env:
  FREELLMAPI_PORT: 3001
  AI_PROVIDER: freellmapi
  OPENAI_API_BASE: http://127.0.0.1:3002/v1
  AIDER_API_BASE: http://127.0.0.1:3002/v1
  AIDER_ANALYTICS: "false"
  AIDER_CHECK_UPDATE: "false"

jobs:
  agent:
    runs-on: ubuntu-latest
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@v5
        with:
          fetch-depth: 0

      - name: Repair invalid codex gitlink
        run: |
          set -euo pipefail
          if git ls-files --stage -- .codex/.tmp/plugins | grep -q '^160000 '; then
            echo 'INVALID_CODEX_GITLINK=1'
            git rm --cached --force --ignore-unmatch .codex/.tmp/plugins
            rm -rf .codex/.tmp/plugins
            git config user.name github-actions[bot]
            git config user.email 41898282+github-actions[bot]@users.noreply.github.com
            git commit -m 'chore: remove invalid codex plugins gitlink'
            git push origin HEAD:main
            git fetch origin main
            git reset --hard origin/main
            git clean -fdx
            echo 'INVALID_CODEX_GITLINK_REPAIRED=1'
          else
            echo 'INVALID_CODEX_GITLINK=0'
          fi

      - name: Clean runtime files
        run: |
          set -euo pipefail
          rm -rf .codex/.tmp/plugins 2>/dev/null || true
          git fetch origin main
          git reset --hard origin/main
          git clean -fdx
          # Aider runtime files were accidentally committed by an earlier cycle.
          # Keep their repository copies intact for the agent baseline and hide only
          # their runtime mutations from Git until the cycle is complete.
          git update-index --assume-unchanged -- .aider.chat.history.md .aider.tags.cache.v4/cache.db 2>/dev/null || true

      - name: Baseline
        run: |
          set -euo pipefail
          test "$GITHUB_REF_NAME" = main
          test -f AGENTS.md
          test -f android/gradlew
          test -f android/app/build.gradle.kts
          test -f docs/agent-next-task.md
          test -z "$(git status --porcelain)"

      - name: Java
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: '17'
          cache: gradle

      - name: Android
        uses: android-actions/setup-android@v4
        with:
          packages: 'platform-tools platforms;android-35 build-tools;35.0.0'
          accept-android-sdk-licenses: true

      - name: Node
        uses: actions/setup-node@v5
        with:
          node-version: '22'

      - name: Start FreeLLMAPI
        run: |
          set -euo pipefail
          git clone --depth 1 https://github.com/SeyhmusKaya/freellmapi-extended.git /tmp/freellmapi
          cd /tmp/freellmapi
          npm ci
          export ENCRYPTION_KEY="$(node -e "console.log(require('crypto').randomBytes(32).toString('hex'))")"
          export PORT="$FREELLMAPI_PORT"
          npm run dev >/tmp/freellmapi.log 2>&1 & echo $! >/tmp/freellmapi.pid
          for i in $(seq 1 90); do
            curl -fsS "http://127.0.0.1:$FREELLMAPI_PORT/v1/models" >/tmp/models.json 2>/dev/null && break
            sleep 2
          done
          test -s /tmp/models.json || { cat /tmp/freellmapi.log >&2; exit 20; }
          python3 - <<'PY'
          import json
          d=json.load(open('/tmp/models.json'))
          assert d.get('data'), 'No models returned by FreeLLMAPI'
          print('FREELLMAPI_MODELS=',len(d['data']))
          PY
          DB_PATH="$(find /tmp/freellmapi -name freeapi.db -type f -print -quit)"
          test -n "$DB_PATH" || exit 21
          GATEWAY_KEY="$(DB_PATH="$DB_PATH" node - <<'NODE'
          const Database=require('/tmp/freellmapi/node_modules/better-sqlite3');
          const db=new Database(process.env.DB_PATH,{readonly:true});
          const rows=db.prepare("SELECT key,value FROM settings WHERE key LIKE '%unified%' OR key LIKE '%api%key%'").all();
          const c=rows.map(r=>String(r.value||'').trim()).filter(v=>v.length>=20&&!/^your-/i.test(v));
          if(!c.length)process.exit(2); process.stdout.write(c.sort((a,b)=>b.length-a.length)[0]);
          NODE
          )" || { echo 'FreeLLMAPI unified API key not found'; exit 22; }
          test "${#GATEWAY_KEY}" -ge 20
          echo "::add-mask::$GATEWAY_KEY"
          echo "GATEWAY_KEY=$GATEWAY_KEY" >> "$GITHUB_ENV"
          echo 'FREELLMAPI_AUTH_KEY_RESOLVED=true'

      - name: Build ordered free model list
        run: |
          python3 - <<'PY'
          import json
          ids=[m['id'] for m in json.load(open('/tmp/models.json')).get('data',[]) if m.get('id')]
          priority=('coder','coding','devstral','codestral','qwen','deepseek','kimi','gemma','llama')
          out=[]
          for p in priority:
            for x in ids:
              if p in x.lower() and x not in out: out.append(x)
          for x in ids:
            if x not in out: out.append(x)
          open('/tmp/model-candidates','w').write('\n'.join(out[:30]))
          print('FREE_MODEL_CANDIDATES=',len(out[:30]))
          PY

      - name: Local OpenAI proxy
        run: |
          cat >/tmp/proxy.py <<'PY'
          import os
          from http.server import BaseHTTPRequestHandler,ThreadingHTTPServer
          from urllib.request import Request,urlopen
          KEY=os.environ['GATEWAY_KEY']
          class H(BaseHTTPRequestHandler):
            def do_GET(self): self.p()
            def do_POST(self): self.p()
            def p(self):
              n=int(self.headers.get('Content-Length','0')); body=self.rfile.read(n) if n else None
              h={k:v for k,v in self.headers.items() if k.lower() not in ('host','authorization','content-length')}; h['Authorization']='Bearer '+KEY
              try:
                with urlopen(Request('http://127.0.0.1:3001'+self.path,data=body,headers=h,method=self.command),timeout=180) as r:
                  b=r.read(); self.send_response(r.status)
                  for k,v in r.headers.items():
                    if k.lower() not in ('transfer-encoding','content-length','connection'): self.send_header(k,v)
                  self.send_header('Content-Length',str(len(b))); self.end_headers(); self.wfile.write(b)
              except Exception as e:
                self.send_response(502); self.end_headers(); self.wfile.write(str(e).encode())
            def log_message(self,*a): pass
          ThreadingHTTPServer(('127.0.0.1',3002),H).serve_forever()
          PY
          nohup python3 /tmp/proxy.py >/tmp/proxy.log 2>&1 & echo $! >/tmp/proxy.pid
          sleep 1

      - name: Autonomous agent with automatic free-model fallback
        run: |
          set -euo pipefail
          python3 -m pip install --user --upgrade aider-chat
          echo "$HOME/.local/bin" >> "$GITHUB_PATH"
          chmod +x scripts/autonomous-agent.sh
          export OPENAI_API_KEY="$GATEWAY_KEY"
          export AIDER_CHAT_HISTORY_FILE=/tmp/cardiag-aider-chat-history.md
          export AIDER_INPUT_HISTORY_FILE=/tmp/cardiag-aider-input-history
          export AIDER_LLM_HISTORY_FILE=/tmp/cardiag-aider-llm-history
          export AIDER_CACHE_DIR=/tmp/cardiag-aider-cache
          while IFS= read -r model; do
            [ -n "$model" ] || continue
            echo "Trying free coding model: $model"
            export FREELLMAPI_MODEL="$model"
            export AIDER_MODEL="openai/$model"
            rm -rf "$AIDER_CACHE_DIR" /tmp/cardiag-aider-chat-history.md /tmp/cardiag-aider-input-history /tmp/cardiag-aider-llm-history
            git restore -- android/app/src/main/java/dz/cardiag/app/core/road/RoadAssistantService.kt android/app/src/main/java/dz/cardiag/app/core/road/RoadAssistantProviders.kt 2>/dev/null || true
            set +e
            ./scripts/autonomous-agent.sh
            status=$?
            set -e
            if [ "$status" -eq 0 ]; then
              echo "FREE_MODEL_SUCCESS=$model"
              exit 0
            fi
            git restore -- android/app/src/main/java/dz/cardiag/app/core/road/RoadAssistantService.kt android/app/src/main/java/dz/cardiag/app/core/road/RoadAssistantProviders.kt 2>/dev/null || true
            echo "FREE_MODEL_FAILED=$model status=$status; falling back to next free model"
            sleep 8
          done < /tmp/model-candidates
          echo 'All free coding models failed'
          exit 1

      - name: Android validation
        working-directory: android
        run: ./gradlew --no-daemon testDebugUnitTest lintDebug assembleDebug assembleRelease bundleRelease --stacktrace

      - name: Scope and cleanup
        run: |
          set -euo pipefail
          # Restore Aider runtime files and remove assume-unchanged flags before scope checks.
          git update-index --no-assume-unchanged -- .aider.chat.history.md .aider.tags.cache.v4/cache.db 2>/dev/null || true
          git restore -- .aider.chat.history.md .aider.tags.cache.v4/cache.db 2>/dev/null || true
          rm -rf .codex/.tmp/plugins 2>/dev/null || true
          allowed='android/app/src/main/java/dz/cardiag/app/core/road/RoadAssistantService.kt
          android/app/src/main/java/dz/cardiag/app/core/road/RoadAssistantProviders.kt'
          while IFS= read -r f; do
            [ -z "$f" ] || printf '%s\n' "$allowed" | grep -Fxq "$f" || { echo "UNEXPECTED_CHANGE=$f"; exit 40; }
          done < <(git diff --name-only)

      - name: Commit validated code
        run: |
          set -euo pipefail
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add android/app/src/main/java/dz/cardiag/app/core/road/RoadAssistantService.kt android/app/src/main/java/dz/cardiag/app/core/road/RoadAssistantProviders.kt
          git diff --cached --quiet && { echo 'No validated code change'; exit 41; }
          git commit -m 'feat(road): autonomous agent improvement'
          git push origin HEAD:main

      - name: Artifacts
        if: always()
        uses: actions/upload-artifact@v6
        with:
          name: cardiag-freellmapi-results
          path: |
            android/app/build/outputs/apk/release/*.apk
            android/app/build/outputs/bundle/release/*.aab
            agent-state.md
          if-no-files-found: warn
          retention-days: 14

      - name: Diagnostics
        if: failure()
        run: |
          tail -n 200 /tmp/freellmapi.log 2>/dev/null || true
          tail -n 100 /tmp/proxy.log 2>/dev/null || true

      - name: Stop
        if: always()
        run: |
          [ -f /tmp/proxy.pid ] && kill "$(cat /tmp/proxy.pid)" 2>/dev/null || true
          [ -f /tmp/freellmapi.pid ] && kill "$(cat /tmp/freellmapi.pid)" 2>/dev/null || true
